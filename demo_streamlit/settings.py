"""Class containing keys for session state."""


class Settings:  # pylint: disable = (too-few-public-methods)
    """Class with session state keys."""

    selected_sample_index: str = "selected_sample_index"
    message: str = "message"
